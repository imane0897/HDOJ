#include <stdio.h>

int main(void)
{
    double x1, y1, x2, y2, x3, y3, x4, y4;
    while (scanf("%lf%lf%lf%lf%lf%lf%lf%lf", &x1, &y1, &x2, &y2, &x3, &y3, &x4, &y4) != EOF)
    {
        // judge intersect
        if ((x3 >= x1 && x3 <= x2))
        {
            
        }
        // calculate area 
    }
    return 0;
}